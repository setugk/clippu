services:
  clipboard:
    build: .
    container_name: clipboard
    restart: unless-stopped
    ports:
      - "5050:5000"
    volumes:
      - ./data:/data
    environment:
      - CLIPPU_USER=admin
      - CLIPPU_PASS=changeme
